import React from 'react';
import Product from '../components/Product';

const ProductView = () => {
    return (
        <div>
            <Product />
        </div>
    );
};

export default ProductView;